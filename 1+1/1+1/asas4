#include<stdio.h>
 main()
{
int n, i, j;
printf("Enter the value of n: ");
scanf("%d",&n);
printf("Multiplication table of %d: \n", n);
for(i=1; i<=12; i++)
{
printf("%d X %d = %d\n", n, i, n*i);
}
}
